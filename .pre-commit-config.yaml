repos:
  - repo: local
    hooks:
      - id: validate-content
        name: validate content JSON
        entry: bash -c "cd scripts/validate-content && npm ci && npm run validate"
        language: system
        pass_filenames: false
        always_run: true
