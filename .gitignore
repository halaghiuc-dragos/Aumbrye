# =============================================================================
# Secrets & credentials
# =============================================================================
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
credentials.json
secrets.json
**/secrets/
**/secrets.yaml
**/secrets.yml
id_rsa
id_rsa.*
.netrc
auth.json
service-account*.json
*-credentials.json
.aws/
.azure/

# =============================================================================
# Environment files
# =============================================================================
.env
.env.*
!.env.example
!.env.*.example

# Vite / frontend local overrides (committed templates: .env.example)
.env.local
.env.*.local

# direnv / shell env loaders (may export secrets)
.envrc
.direnv/

# Azure Functions local settings
local.settings.json

# =============================================================================
# ASP.NET configuration (use User Secrets or env vars for local overrides)
# =============================================================================
**/appsettings.*.json
!**/appsettings.json
!**/appsettings.Testing.json

# =============================================================================
# Docker local overrides
# =============================================================================
docker-compose.override.yml
docker-compose.*.local.yml

# =============================================================================
# OS junk
# =============================================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# =============================================================================
# IDE / editor
# =============================================================================
.vs/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
.fleet/
*.swp
*.swo
*~
*.DotSettings.user
*.rsuser
.mono/

# Cursor — ignore local machine config; allow shared rules if added later
.cursor/*
!.cursor/rules/
!.cursor/rules/**
.cursor/mcp.json

# =============================================================================
# Node / JavaScript
# =============================================================================
node_modules/
dist/
build/
.cache/
.parcel-cache/
.turbo/
.vite/
.npm
.npmrc
.yarnrc.yml
coverage/
.nyc_output/
.eslintcache
*.tsbuildinfo
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# =============================================================================
# .NET
# =============================================================================
bin/
obj/
out/
artifacts/
publish/
TestResults/
*.user
*.suo
*.userosscache
*.sln.docstates
*.nupkg
project.lock.json

# =============================================================================
# Godot
# =============================================================================
**/.godot/
**/.import/
**/export_presets.local.cfg
override.cfg
*.translation

# Godot export binaries (signing credentials live in export_presets.local.cfg)
**/export/
**/builds/
*.pck
*.apk
*.aab

# Godot user-data equivalents if copied into the repo by mistake
aumbrye_save.json
godot_mcp_settings.json
mcp_validation.json

# Local Godot dev config overrides
**/config/dev_api.local.json

# =============================================================================
# =============================================================================
# Steam / platform keys
# =============================================================================
steam_appid.txt
steam_api_key.txt
# GodotSteam binaries (gdextension config is committed)
**/addons/godotsteam/**/*.dll
**/addons/godotsteam/**/*.so
**/addons/godotsteam/**/*.dylib

# =============================================================================
# Build artifacts & binaries
# =============================================================================
*.dll
*.exe
*.pdb

# =============================================================================
# Generated reports, logs, and local test dumps
# =============================================================================
reports/
logs/

# Procgen CLI dumps at repo root (regenerate locally)
/seed*.json

# Local capture dumps
.tmp_capture/

# Local database files (dev dumps, accidental copies)
*.sqlite
*.sqlite3
*.db

# =============================================================================
# Python caches (scripts/, tooling)
# =============================================================================
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# =============================================================================
# Terraform / infra state (if added later)
# =============================================================================
.terraform/
*.tfstate
*.tfstate.*

# =============================================================================
# Temp / merge artifacts
# =============================================================================
*.orig
*.rej
*.bak
*.tmp
*.temp

# =============================================================================
# Assets (large binaries — track via LFS or external pipeline later)
# =============================================================================
# Uncomment when asset pipeline is defined:
# assets/**/*.import

# =============================================================================
# Generated repository inventories (regenerate on demand with `git ls-files`)
# =============================================================================
project_structure.txt
