# Batch 8 documentation prep

**Prepared:** 2026-08-06  
**Coordinator scope:** Next 10 unfinished topics after Batch 7 (`validation-suites` → `diorama-weapon-kit`).

---

## FINISHED status grep

Searched all candidate paths for `## Status: FINISHED` and `**Status: FINISHED**`.

| Topic | Status | Notes |
|-------|--------|-------|
| `audio-director.md` | **FINISHED** | `## Status: FINISHED` — skip per task |
| `pixel-diorama-settings.md` | **FINISHED** | `**Status: FINISHED**`; existing twin marks improvement plan FINISHED |
| All other Batch 8 candidates | Unfinished | No status header; acceptance criteria unchecked |

---

## Final Batch 8 topic list (10)

| # | Topic | Gap prefix | Substitution |
|---|-------|------------|--------------|
| 1 | `pixel-style.md` | PXS | **Substitute** for finished `pixel-camera-snap.md` |
| 2 | `diorama-anim-controller.md` | ANC | — |
| 3 | `visual-lighting.md` | LIT | — |
| 4 | `vfx-service.md` | VFX | — |
| 5 | `portal-ellipse-shader.md` | POR | — |
| 6 | `character-floor-snap.md` | SNP | — |
| 7 | `ui/combat_hud.md` | HUD | — |
| 8 | `ui/minimap.md` | MAP | — |
| 9 | `ui/game_ui_skin.md` | GSK | **Substitute** for finished `material-dissolve.md` (batch 7) |
| 10 | `ui/status_icon_atlas.md` | SIA | UI subtree continuation |

**Batch 7 closed:** all 10 topics FINISHED (`validation-suites` through `diorama-weapon-kit`).

---

## Per-topic prep notes

### 1. `pixel-camera-snap.md`

| Twin | State | Completeness |
|------|-------|--------------|
| `actual_improvements/` | Full improvement plan (7 gaps PCS-01..07, target design, work plan, validation spec) | ~90% |
| `existing_codebase/` | Files, how-it-works, contracts, current-state table | ~85% |

**Gaps filled:** Current state, gap table, target design (world-axis snap, rotation snap, live focus distance), work plan, acceptance criteria (unchecked), planned `pixel_camera_snap_suite.gd`.

**Missing / thin:** No `## Status` header; no post-implementation sync; existing twin does not yet document planned APIs (`snap_origin`, `snap_basis`, `rotation_step_radians`).

**Key code paths:**
- `apps/game/client/scripts/art/pipeline/pixel_camera_snap.gd`
- `apps/game/client/scripts/art/pipeline/pixel_diorama_viewport.gd` (`_mirrored_transform`, `SNAP_FOCUS_DISTANCE`)
- `apps/game/client/scripts/art/pipeline/pixel_diorama_settings.gd` (`camera_snap_step`, `camera_snap_enabled`, `active_render_height`)
- `apps/game/client/scripts/camera/orbit_camera.gd` (unsnapped gameplay rig)

**Validation:** `pixel_pipeline_suite.gd` — file existence only (`:18`). Planned: **`pixel_camera_snap_suite.gd`** (graphics).

---

### 2. `pixel-style.md`

| Twin | State | Completeness |
|------|-------|--------------|
| `actual_improvements/` | 14 gaps PXS-01..14, palette-as-data target, work plan | ~90% |
| `existing_codebase/` | Deep inventory (shaders, palette, factories, hub tent, current-state table) | ~90% |

**Gaps filled:** Comprehensive gap analysis, target design (`content/art/palettes.json`), work plan, validation spec.

**Missing / thin:** PDS-01 fix in settings may partially address PXS-02 (`restamp_tracked`); verify PXS-02 still accurate after settings FINISHED work. No `pixel_style_suite.gd` yet.

**Key code paths:**
- `apps/game/client/scripts/art/style/pixel_diorama_style.gd`
- `apps/game/client/assets/shared/pixel_diorama_surface.gdshader` (`color_levels` dead uniform — PXS-01)
- `apps/game/client/assets/shared/pixel_diorama_emissive.gdshader`
- `apps/game/client/assets/shared/pixel_diorama_finish.gdshaderinc`
- `apps/game/client/assets/<theme>/mat_*.tres` (30 biome materials)
- `apps/game/client/scripts/art/pipeline/pixel_diorama_settings.gd` (`apply_to_shader_material`, `track`)

**Validation:** `pixel_pipeline_suite.gd` — shader file existence + `flash_amount`/`dissolve_clip` substrings. Planned: extend `pixel_pipeline_suite.gd` + **`pixel_style_suite.gd`**.

---

### 3. `diorama-anim-controller.md`

| Twin | State | Completeness |
|------|-------|--------------|
| `actual_improvements/` | 14 gaps ANC-01..14, events-path fix, mirror API | ~90% |
| `existing_codebase/` | Binding table, priority stack, API table, current-state | ~85% |

**Gaps filled:** Root-cause analysis of `_events_path == ""`, target design, controller validation extensions.

**Missing / thin:** Cross-doc ANL-01 (anim library exporter path table) dependency; verify `diorama-anim-library.md` Batch 7 state.

**Key code paths:**
- `apps/game/client/scripts/art/characters/diorama_anim_controller.gd`
- `apps/game/client/scripts/art/characters/diorama_anim_library.gd` (`_compile` method track)
- `apps/game/client/scripts/player/player_anim_director.gd`
- `apps/game/client/scripts/player/locomotion.gd` (footstep fallback suppression)
- `apps/game/client/scripts/enemies/castle_enemy_base.gd`
- `apps/game/client/scripts/combat/weapon_controller.gd` (hitbox anim hooks)

**Validation:** **`diorama_anim_suite.gd`** (extend with controller-level headless tests per improvement plan).

---

### 4. `visual-lighting.md`

| Twin | State | Completeness |
|------|-------|--------------|
| `actual_improvements/` | 11 gaps LIT-01..11, `content/art/lighting.json` target | ~85% |
| `existing_codebase/` | Outdoor presets, indoor path, atmosphere; thinner on flicker/animation | ~80% |

**Gaps filled:** Dual data-source problem documented, unified profile schema, work plan.

**Missing / thin:** Per-biome grade table now lives in settings/biome JSON (post-PDS); reconcile LIT references with `pixel-diorama-settings` FINISHED state.

**Key code paths:**
- `apps/game/client/scripts/art/lighting/visual_lighting.gd`
- `apps/game/client/assets/shared/pixel_sky.gdshader`
- `apps/game/client/scripts/content/biome_registry.gd` (`get_lighting_profile`, `apply_indoor_environment`, sun hide)
- `apps/game/client/scripts/dungeon/diorama_room_dressing.gd` (`configure_soft_omni` callers)
- `apps/game/client/scripts/hub/hub_diorama.gd`, `arena_diorama.gd`, `waves_outdoors_diorama.gd`

**Validation:** `pixel_pipeline_suite.gd` (script exists), `m5_suite.gd` / `m6_suite.gd` (biome `ambient_color` only). Planned: **`visual_lighting_suite.gd`** or extend `pixel_pipeline_suite.gd`.

---

### 5. `vfx-service.md`

| Twin | State | Completeness |
|------|-------|--------------|
| `actual_improvements/` | 13 gaps VFX-01..13, `content/vfx/effects.json` target | ~85% |
| `existing_codebase/` | API table, pool mechanics; ~110 lines | ~75% |

**Gaps filled:** Content-data target, hit-stop/shake design, telegraph consolidation plan.

**Missing / thin:** Existing twin lacks `play_telegraph` dead-code section depth; AUD-11 (audio from VFX) may overlap `audio-director` FINISHED work — verify current `vfx_service.gd` audio calls.

**Key code paths:**
- `apps/game/client/scripts/art/vfx/vfx_service.gd` (autoload)
- `apps/game/client/scripts/art/characters/material_dissolve.gd` (`DEATH_BURST_LIFETIME` comment)
- Callers: `weapon_controller.gd`, `hurtbox.gd`, `guard.gd`, `hitbox.gd`, `locomotion.gd`, `player_anim_director.gd`, `castle_enemy_base.gd`
- `apps/game/client/scripts/art/style/pixel_diorama_style.gd` (fountain particles bypass — PXS-08 overlap)

**Validation:** `perf_gate_suite.gd` (grep `_burst_pool`, frame budget stub). Planned: **`vfx_service_suite.gd`** + real perf gate.

---

### 6. `portal-ellipse-shader.md`

| Twin | State | Completeness |
|------|-------|--------------|
| `actual_improvements/` | 10 gaps POR-01..10, `content/art/portals.json`, unified `build_portal` | ~85% |
| `existing_codebase/` | Shader walkthrough, theme table, consumer map | ~80% |

**Gaps filled:** Run-vs-hub portal split, POR-02 runtime error on `glow.emission`, content schema.

**Missing / thin:** POR-02 overlaps DRD-01 / `diorama_interactable_skin.gd` — coordinate with room-dressing if both batched.

**Key code paths:**
- `apps/game/client/assets/shared/portal_ellipse.gdshader`
- `apps/game/client/scripts/art/style/pixel_diorama_style.gd` (`make_portal_material`, `add_portal_interior`, `dress_portal_architecture`)
- `apps/game/client/scripts/art/characters/diorama_interactable_skin.gd` (`build_portal` — broken `glow.emission`)
- `apps/game/client/scripts/hub/hub_diorama.gd`, `arena_diorama.gd`
- `apps/game/client/scripts/dungeon/room_merchant_content.gd` (merchant stall misuses portal)

**Validation:** `hub_suite.gd`, `hub_m4_suite.gd`, `m7_suite.gd`, `dungeon_suite.gd` (node names only). Planned: **`portal_shader_suite.gd`** or extend `pixel_pipeline_suite.gd`.

---

### 7. `character-floor-snap.md`

| Twin | State | Completeness |
|------|-------|--------------|
| `actual_improvements/` | 8 gaps SNP-01..08, world-space target design | ~85% |
| `existing_codebase/` | Caller table, player vs enemy convention table | ~85% |

**Gaps filled:** Coordinate-space root cause, raycast floor probe plan, validation extensions.

**Missing / thin:** `feet_local_y` stub cross-ref to `diorama_character_skin.gd`; only `castle_run.gd:345` uses raycast today.

**Key code paths:**
- `apps/game/client/scripts/art/characters/character_floor_snap.gd`
- `apps/game/client/scripts/art/characters/diorama_character_skin.gd` (`feet_local_y`)
- `apps/game/client/scripts/dungeon/dungeon_builder.gd`, `castle_run.gd`, `waves_run.gd`
- `apps/game/client/scripts/enemies/castle_enemy_base.gd` (`align_diorama_visual`)

**Validation:** None today. Planned: extend **`player_suite.gd`** + **`enemy_suite.gd`** per improvement plan.

---

### 8. `material-dissolve.md` *(fallback #8)*

| Twin | State | Completeness |
|------|-------|--------------|
| `actual_improvements/` | 12 gaps DIS-01..12, directional dissolve target | ~80% |
| `existing_codebase/` | Shader discard, dissolve/restore flow, player/enemy callers | ~75% |

**Gaps filled:** Respawn restore bug (DIS-01), emissive non-dissolve (DIS-03), MaterialFlash interaction (DIS-06).

**Missing / thin:** Post-PDS `restamp_tracked` may affect DIS-12 — re-verify. Existing twin short on revive/respawn paths.

**Key code paths:**
- `apps/game/client/scripts/art/characters/material_dissolve.gd`
- `apps/game/client/assets/shared/pixel_diorama_surface.gdshader` (`dissolve_clip` discard)
- `apps/game/client/assets/shared/pixel_diorama_emissive.gdshader` (no `dissolve_clip` — DIS-03)
- `apps/game/client/scripts/art/characters/material_flash.gd` (interaction DIS-06)
- `apps/game/client/scripts/player/player_combat_reactions.gd`
- `apps/game/client/scripts/enemies/castle_enemy_base.gd` (death + `respawn_at_rest`)

**Validation:** `pixel_pipeline_suite.gd` (shader substring `dissolve_clip` only). Planned: **`material_dissolve_suite.gd`** or extend `pixel_pipeline_suite.gd`.

---

### 9. `ui/combat_hud.md`

| Twin | State | Completeness |
|------|-------|--------------|
| `actual_improvements/` | 14 gaps HUD-01..14, authored `combat_hud.tscn` target | ~90% |
| `existing_codebase/` | Scene coverage table, per-feature current state | ~85% |

**Gaps filled:** Scene fragmentation analysis, status pip design, validation table for `m5_suite.gd`.

**Missing / thin:** Depends on `status_icon_atlas.md`, `input_glyphs.md`, `game_ui_skin.md` assets; waves HUD built from bare `Control`.

**Key code paths:**
- `apps/game/client/scripts/ui/combat_hud.gd`
- `apps/game/client/scripts/ui/minimap.gd` (hosted widget)
- `apps/game/client/scripts/ui/status_icon_atlas.gd`
- Scenes: `castle_run.tscn`, `hub.tscn`, `combat_arena.tscn`, `forgotten_castle_slice.tscn`
- `apps/game/client/scripts/dungeon/waves_run.gd` (runtime HUD)
- `apps/game/client/scripts/dungeon/castle_run.gd` (minimap configure)

**Validation:** `m5_suite.gd` (partial `combat_hud.gd` grep). Planned: extend **`m5_suite.gd`** per improvement plan table.

---

### 10. `ui/minimap.md`

| Twin | State | Completeness |
|------|-------|--------------|
| `actual_improvements/` | 10 gaps MAP-01..10, reveal tiers, full-map overlay | ~90% |
| `existing_codebase/` | Data intake, draw order, current-state table | ~85% |

**Gaps filled:** Edge fog inversion (MAP-01), player marker, procgen `kind`/`size` schema.

**Missing / thin:** Procgen schema change (step 6) is a cross-cutting dependency on `room_graph_geometry.gd` / `dungeon-definition.v1.json`.

**Key code paths:**
- `apps/game/client/scripts/ui/minimap.gd`
- `apps/game/client/scripts/ui/combat_hud.gd` (instantiate + forward API)
- `apps/game/client/scripts/dungeon/castle_run.gd` (sole `configure` caller)
- `apps/game/client/scripts/dungeon/procgen/dungeon_procgen.gd`
- `apps/game/client/scripts/dungeon/procgen/room_graph_geometry.gd`

**Validation:** None dedicated. Planned: extend **`room_graph_suite.gd`** per improvement plan.

---

## Batch 8 validation suite map

| Topic | Existing coverage | Planned / named in improvement plan |
|-------|-------------------|-------------------------------------|
| pixel-camera-snap | `pixel_pipeline_suite.gd` (path only) | `pixel_camera_snap_suite.gd` |
| pixel-style | `pixel_pipeline_suite.gd` (shaders) | `pixel_style_suite.gd` |
| diorama-anim-controller | `diorama_anim_suite.gd` | extend same |
| visual-lighting | `pixel_pipeline_suite.gd`, `m5`/`m6` ambient | `visual_lighting_suite.gd` (TBD) |
| vfx-service | `perf_gate_suite.gd` (stubs) | `vfx_service_suite.gd` |
| portal-ellipse-shader | `hub_suite`, `m7`, `dungeon` (names) | `portal_shader_suite.gd` (TBD) |
| character-floor-snap | none | `player_suite.gd` + `enemy_suite.gd` |
| material-dissolve | `pixel_pipeline_suite.gd` (substring) | `material_dissolve_suite.gd` (TBD) |
| combat_hud | `m5_suite.gd` (partial) | extend `m5_suite.gd` |
| minimap | none | extend `room_graph_suite.gd` |

---

## Suggested worker order

1. **Foundation art** (shared deps): `pixel-style` → `visual-lighting` → `vfx-service` (palette/settings/lighting/VFX chain).
2. **Pipeline snap**: `pixel-camera-snap` (depends on `pixel-diorama-pipeline` PDP-03 focus distance — FINISHED).
3. **Character presentation**: `diorama-anim-controller` → `character-floor-snap` → `material-dissolve`.
4. **Portal**: `portal-ellipse-shader` (touches `pixel_diorama_style` + interactable skin).
5. **UI subtree**: `combat_hud` → `minimap` (minimap hosted by HUD; do HUD scene first).

---

## Skipped (already FINISHED)

- `audio-director.md` — per task brief
- `pixel-diorama-settings.md` — synced twins; replaced by `material-dissolve.md` in this batch
