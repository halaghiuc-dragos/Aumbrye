# Project Audit — 2026-08

Format: **Problem → Action → Location**, plus **Tool / Tech** where a specific technology is recommended.
Severity: `P0` breaks the game · `P1` clearly wrong · `P2` polish/debt.

| # | Section |
|---|---------|
| 1 | [Bugs](#1-bugs) |
| 2 | [Combat — soulslike feel](#2-combat--soulslike-feel) |
| 3 | [Room generation](#3-room-generation) |
| 4 | [Story & narrative](#4-story--narrative) |
| 5 | [Class, starting weapon & build](#5-class-starting-weapon--build) |
| 6 | [Gameplay & loop](#6-gameplay--loop) |
| 7 | [Graphics & VFX](#7-graphics--vfx) |
| 8 | [Audio](#8-audio) |
| 9 | [General project health](#9-general-project-health) |
| 10 | [Suggested order](#10-suggested-order) |

---

## 1. Bugs

### 1.1 Procedural generation

| Sev | Problem | Action | Location |
|-----|---------|--------|----------|
| P0 | Bbox fill creates 2×2 blocks, then validation rejects 2×2 blocks — every attempt fails, so **every dungeon falls back to a fixed layout** | Run the 2×2 check on the walk graph only, before fill; or set `allow_2x2_blocks = true` when `fill_bounding_box` is on — **FIXED** (filler cells excluded from 2×2 validation; fillers no longer receive door masks) | `room_graph_generator.gd:93-97`, `:340-343`, `room_graph_config.gd:15`, `:19` |
| P0 | Fallback graph takes no RNG — same layout for every seed, floor, and biome | Pass `rng` into `_build_fallback_graph`; randomise leg length and turn order — **FIXED** | `room_graph_generator.gd:42`, `:347-394` |
| P0 | Tests only assert same-seed determinism and `rooms >= 8` — a constant layout passes all of them | Add "different seeds → different layout signature"; assert `used_fallback == false` — **FIXED** | `room_graph_suite.gd:21-42`, `:90-116` |
| P1 | `used_fallback` is plumbed through but never checked anywhere | Fail validation on it; warn at runtime — **FIXED** (runtime warning + suite assertions) | `dungeon_procgen.gd:85`, `room_graph_generator.gd:45` |
| P1 | Floor seed mixed twice on the CLI path (GDScript mixes, then C# mixes again) | Pass the pre-floor `tier_seed`, or pass `--floor 1` with the already-mixed seed — **FIXED** (CLI receives `tier_seed`) | `local_procgen.gd:30`, `:56-57`, `packages/procedural/Generation/DungeonGenerator.cs:41` |
| P1 | Door mismatches only `push_warning` — broken geometry ships silently | Promote to hard failure and retry generation — **FIXED** (`validate_door_topology` + assignment retry loop) | `room_graph_geometry.gd:198-218` |
| P1 | Secret rooms never get a door mask (skipped in the connection pass) but are still placed | Set the secret's mask toward its parent — **FIXED** (`_apply_secret_door_masks`) | `room_graph_generator.gd:149-158`, `:266-268` |
| P1 | Boss door hardcoded to the NORTH socket — wrong when the boss room is rotated | Derive the socket from the room's yaw — **FIXED** (`_boss_approach_socket`) | `dungeon_builder.gd:512-518` |
| P1 | Backend generator always generates floor 1 | Thread `floorIndex` through `RunService` — **IMPLEMENTED** (`IDungeonGenerator` + `DungeonGenerator` accept `floorIndex`) | `services/backend/.../RunService.cs:203-214` |
| P2 | `boss_candidates[0]` indexes an empty array if the walk places zero rooms | Guard empty, return `null` to retry — **FIXED** | `room_graph_generator.gd:202-215` |
| P2 | Dead loop body — `for slot_id in ...: if ...: continue` does nothing | Delete — **FIXED** | `room_graph_generator.gd:232-234` |
| P2 | Stairs room = first neighbour in N/E/S/W order; C# requires south | Pick deliberately, match the C# rule — **FIXED** (prefer south neighbor) | `room_graph_generator.gd:225-231` |
| P2 | `obstacle_id` declared but never assigned — obstacle rooms are dead code | Wire in Phase 1 or remove — **FIXED** (removed unused field) | `room_graph.gd:12` |
| P2 | Non-deterministic run when seed is null | Log the rolled seed so runs stay reproducible — **FIXED** | `local_procgen.gd:139-142` |

### 1.2 Combat

| Sev | Problem | Action | Location |
|-----|---------|--------|----------|
| P0 | **Bow can never fire** — `DRAWING` sets `is_attacking`, next frame routes to `_process_attack_phase`, whose `match` has no `DRAWING` case, so charge stays at one frame (~0.02, below the 0.05 release threshold) and the release check is never reached again | **FIXED** — drawing handled separately from attack phase transitions | `weapon_controller.gd:221-235`, `:173-188` |
| P0 | `Poise._broken` never clears — after one poise break a character can never be staggered again for the rest of its life | **FIXED** — `_broken` clears on regen; `reset_poise()` on `stagger_ended` | `poise.gd:41-49`, `player_combat_reactions.gd:39-41` |
| P1 | Attacks allowed mid-dodge and mid-block — no mutual exclusion | **FIXED** — `is_dodging` / `is_guard_active` in `_is_action_blocked()` | `weapon_controller.gd:287-294` |
| P1 | `DODGE_RECOVERY` (0.35s) computed but never enforced — `Dodge.locks_movement()` has no callers | **FIXED** — dodge recovery in `is_movement_locked()` | `dodge.gd:11`, `:72-73`, `player_combat_reactions.gd:48-53` |
| P1 | Lock-on target switch reassigns `current_target` without rewiring the `died` signal — the old target's death steals the lock | **FIXED** — switch routes through `_set_lock()` | `lock_on.gd:137-140` vs `:59-67` |
| P1 | Guard break uses class constant `MAX_POISE` (50) instead of the instance's `max_poise` | **FIXED** — uses `_poise.max_poise` | `guard.gd:161` |
| P1 | Equipment `defense` and `staminaMax` are aggregated but never applied to the player | **FIXED** — defense meta + `Stamina.configure()` | `inventory_service.gd:153-171`, `stamina.gd:8-12` |
| P1 | Hitstop plays only on the attacker, never the victim | **FIXED** — victim `HitFeedback.on_hit_received` from `Hurtbox` | `hitbox.gd:145-147` |
| P2 | Global `Engine.time_scale` hitstop slows UI and every other system | **FIXED** — per-actor animation speed scale | `hit_feedback.gd:66-74` |
| P2 | Light combo index never times out — the chain persists across arbitrary idle gaps | **FIXED** — `_combo_idle_timer` resets chain | `weapon_controller.gd:212-218` |
| P2 | Animation hitbox markers (`anim_hitbox_on/off`) are empty no-ops | **FIXED** — wired to weapon hitbox enable/disable | `diorama_anim_controller.gd:324-329` |
| P2 | Sprint calls `consume(delta)` every frame, resetting the regen delay continuously | **FIXED** — sprint uses `drain()` without regen delay | `locomotion.gd:91-93`, `stamina.gd:34` |
| P2 | `_stamina.consume` called with no null check in the block path | **FIXED** — null guard before consume | `guard.gd:100` |
| P2 | Guard break tracks the same window twice (`_stagger_timer` and an `await` timer) | **FIXED** — removed duplicate `await` | `guard.gd:156-165` |
| P2 | Boss phase check divides by `max_value` with no zero guard | **FIXED** — `max_value > 0` guard | `crystal_sovereign.gd:112-113` |
| P2 | Training grunt "death" resets the dummy instead of dying | **FIXED** — arena resets dummies; grunt enters DEAD state | `training_grunt.gd:218-219` |

### 1.3 Progression & content wiring

| Sev | Problem | Action | Location |
|-----|---------|--------|----------|
| P0 | **The entire talent tree grants nothing** — code reads `node["stats"]`, JSON writes `node["effects"][{stat, valuePerRank}]`, so totals are always empty | Parse the `effects` array, multiply `valuePerRank` by rank | `progression_service.gd:112-127` vs `content/talents/tree.json:15` | **FIXED** |
| P1 | Talent UI reads the same wrong key, so nodes display no stats | Same fix as above | `talents_ui.gd:140` | **FIXED** |
| P1 | Even once parsed, 9 of 11 talent stats have no consumer (`physicalDamage`, `critChance`, `poiseDamage`, `armor`, `blockReduction`, `poise`, `staminaRegen`, `staminaCostReduction`, `damageReduction`) | Extend `STAT_KEYS` and apply each in the damage/stamina pipeline | `items/equipment.gd:11-13` | **FIXED** |
| P1 | ~13 of 20 weapon items have no `weaponId`, so they all play the `sword_basic` moveset | Add `weaponId` to every weapon item, or fail content validation without it | `content/items/equipment/*.json`, `grid_inventory.gd:315-321` | **FIXED** |
| P1 | Item stats use `physicalDamage` / `fireDamage`, which aren't in `STAT_KEYS` — bonuses silently do nothing | Unify the stat vocabulary; validate stat keys in `validate.mjs` | `items/equipment.gd:11-13`, `content/items/equipment/iron_sword.json:11-13` | **FIXED** |
| P2 | Level tracked in both `CharacterService` and `ProgressionService` | Single owner, derive the other | `character_service.gd:16`, `progression_service.gd:57-58` | **IMPLEMENTED** — `ProgressionService` owns level; `CharacterService.level` derives via getter |
| P2 | Only one material in all of `content/` has a `runRelicId`, so 11 relics are nearly unreachable | Add relic drops to loot tables | `content/relics/`, `inventory_service.gd:34-38` | **IMPLEMENTED** — relic materials per biome in `procgen_loot_tables.gd` |
| P2 | Talent nodes use `nameKey` only; UI also reads `name` | Add a translation table, or add `name` fields | `talents_ui.gd:125`, `:146` | **IMPLEMENTED** — `nameKey` via `tr()` in `strings.csv` |

### 1.4 Settings & feedback wiring

| Sev | Problem | Action | Location | Status |
|-----|---------|--------|----------|--------|
| P1 | `reduce_camera_shake` has a working toggle but **no consumer** — camera punch always fires | Check it in `_apply_camera_punch()` | `accessibility_settings.gd:9`, `hit_feedback.gd:77-83` | **FIXED** |
| P1 | `ui_scale` is persisted but never applied to any UI root | Apply to `CanvasLayer` scale, or set `content_scale_factor` on the window | `settings_ui.gd:94-104` | **FIXED** (`DisplaySettings` → `content_scale_factor`) |
| P1 | `subtitle_scale` and `vibration_intensity` are stored with no slider and no runtime effect | Wire subtitle scale to `dialogue_ui`; call `Input.start_joy_vibration()` from `HitFeedback` | `accessibility_settings.gd:11-12` | **FIXED** |
| P2 | Combat SFX hook is an empty `pass` — every hit is silent | See [§8](#8-audio) | `hit_feedback.gd:86-87` | **FIXED** (`AudioDirector.play_combat_sfx`) |
| P2 | `AudioDirector.stop_all(fade)` ignores its fade argument and hard-stops | Tween `volume_db` before stopping | `audio_director.gd:73-76`, `:114-115` | **FIXED** (already tweens via `_fade_out_player`) |

### 1.5 Other

| Sev | Problem | Action | Location | Status |
|-----|---------|--------|----------|--------|
| P1 | Save migration failure returns the **unmodified** save — corrupt data loads silently | Refuse to load, back up the file, surface an error | `save_migrator.gd:56-58` | **FIXED** |
| P2 | Missing content JSON returns `{}` with only a warning | Fail fast in debug builds | `content_loader.gd:12-14` | **FIXED** |
| P2 | Dev password hardcoded in the client | Move to an env var / dev-only config | `net/api_client.gd:42` | **FIXED** (`AUMBRYE_DEV_PASSWORD` / `dev_api.json`) |
| P2 | `.tmp_capture/` (58 files) untracked and not gitignored | Add to `.gitignore` | `.gitignore` | **FIXED** |
| P2 | `hub_stub.gd` / `hub_stub.tscn` orphaned | Delete | `scenes/hub/hub_stub.*` | **FIXED** (already absent) |

---

## 2. Combat — Soulslike Feel

### 2.1 Commitment (highest impact)

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| Attacks are cancellable by anything — no weight | **IMPLEMENTED** — movement lock through startup/active/recovery; late-recovery dodge cancel | `weapon_controller.gd:173-188` | GDScript state gate; drive from `AttackPhase` |
| Full move speed while swinging | **IMPLEMENTED** — ~20% speed during startup/active | `locomotion.gd:82-98` | reuse `set_speed_multiplier()` |
| No turn cap while attacking | **IMPLEMENTED** — rotation cap during startup/active | `locomotion.gd:111-113` | `lerp_angle` with a per-phase max delta |
| Recovery is not felt — no whiff punish | **IMPLEMENTED** — recovery lock enforced from JSON timings | `weapon_controller.gd:182-187` | JSON `recovery` already exists; enforce it |
| Movement fully code-driven, animations cosmetic | **IMPLEMENTED** — attack lunge displacement during active frames from weapon JSON | `weapon_controller.gd`, `locomotion.gd` | `lunge_distance` per attack in weapon JSON |

### 2.2 Defence

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| Block is a tap with a fixed 0.65s window — no hold | **IMPLEMENTED** — hold-to-block while `block` held | `guard.gd:39-65` | extend `GuardState` enum |
| Mistimed parry = full damage (no block during the parry window) | **IMPLEMENTED** — parry window is first 0.18s of active guard | `guard.gd:52-56`, `:95-99` | — |
| Parry only staggers — no payoff | **IMPLEMENTED** — riposte window with 2× damage multiplier | `guard.gd:110-119` | `Area3D` prompt + `play_attack` clip |
| No backstab / positional crit | **IMPLEMENTED** — rear-arc 1.5× damage multiplier | `hurtbox.gd:30-51` | dot product vs victim facing |
| Parry is a single-frame check at enemy attack start | **IMPLEMENTED** — parry tested in `Hurtbox.receive_hit` during active frames | `castle_enemy_base.gd:497-514` | move the check into `Hitbox._try_hit` |
| Projectiles can only be dodged, never blocked | **IMPLEMENTED** — projectile hits route through `modify_incoming_hit` | `enemy_projectile.gd:16-51` | — |

### 2.3 Resources & risk

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| Healing requires opening the inventory — zero risk | **IMPLEMENTED** — H key estus: 3 charges, vulnerable drink animation | `player_heal.gd`, `project.godot` | new `heal` action in `project_input`; `drink` clip in `diorama_anim_library.gd` |
| Stamina max is a hard-coded constant | **IMPLEMENTED** — `configure()` driven from equipment | `stamina.gd:8-12` | — |
| Zero stamina has no penalty state | **IMPLEMENTED** — exhaustion until 15 stamina regained | `stamina.gd:29-38` | — |
| Poise is invisible to the player | **IMPLEMENTED** — flinch tiers on poise damage + stagger telegraph | `poise.gd`, `player_anim_director.gd` | reuse `flinch` clip already in the anim library |
| Hyperarmor is greatsword-only and binary | **IMPLEMENTED** — per-attack `hyperarmor` / `poise_threshold` in weapon data | `weapon_controller.gd:200` | move to weapon JSON |

### 2.4 Camera & targeting

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| Camera does not reorient on lock-on | **IMPLEMENTED** — damped yaw blend toward player+target midpoint | `orbit_camera.gd`, `lock_on.gd:96-111` | `SpringArm3D` + damped `lerp_angle` |
| Lock breaks hard on any LOS loss | **IMPLEMENTED** — 0.75s LOS grace timer | `lock_on.gd:109-111` | — |
| No soft-lock for unlocked attacks | **IMPLEMENTED** — cone snap on attack start | `weapon_controller.gd:163-170` | reuse `_find_best_target` |
| Target switch needs full stick deflection (0.7) | **IMPLEMENTED** — deadzone 0.45 + mouse-wheel switching | `lock_on.gd:6`, `:114-140` | — |

### 2.5 Enemy design

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| Enemies have one attack — no reads to learn | **IMPLEMENTED** — `attacks` array in enemy JSON with random selection | `castle_enemy_base.gd:473-526` | array of attacks in enemy JSON |
| No delayed / feint attacks | **IMPLEMENTED** — `windup_variance` per attack | `castle_enemy_base.gd:497-514` | — |
| No combo strings | **IMPLEMENTED** — `combo_followups` chain with gaps | `castle_enemy_base.gd:354-376` | — |
| Missing AI states (investigate, reposition, retreat) | **IMPLEMENTED** — INVESTIGATE + RETREAT states | `castle_enemy_base.gd:6`, `docs/plan/systems/03-ENEMY-AI.md` | consider `AnimationNodeStateMachine` for anim, keep FSM in GDScript |
| No group coordination — all enemies swing at once | **IMPLEMENTED** — `AttackTokenService` autoload (max 2 per group) | `castle_enemy_base.gd` | small autoload or group-scoped semaphore |

### 2.6 Missing systems

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| No bonfire / checkpoint loop | **IMPLEMENTED** — rest room content: heal, refill charges, respawn enemies | `run_flow.gd:345-378` | new room content type in `room_content_types.gd` |
| No two-hand mode | **IMPLEMENTED** — V key toggle with damage/poise scaling | `weapon_controller.gd` | `ShieldMount` visibility + stat multiplier |
| No weapon arts | **IMPLEMENTED** — C key per-weapon `art` block from JSON | `weapon_controller.gd` | weapon JSON `art` block |
| Bow has no aim mode | **IMPLEMENTED** — hold block/light to aim; P0 draw fix | `weapon_controller.gd:221-235` | `Control` reticle on the HUD `CanvasLayer` |

---

## 3. Room Generation

> Fix the two P0 generation bugs first — until then every dungeon is one fixed layout and nothing below is observable.

### 3.1 Layout structure

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| Bbox fill turns the layout into a solid rectangle where every neighbour gets a door — reads as one open blob, not a dungeon | Drop the fill, or fill without auto-connecting every adjacency — **FIXED** (fillers excluded from connection pass) | `room_graph_generator.gd:285-307`, `:143-158` | — |
| Doors derived purely from grid adjacency — no intentional walls | Assign edges from the walk tree plus a controlled number of extra loops — **IMPLEMENTED** (spanning-tree walk edges + `loop_budget` extra adjacency doors) | `room_graph_generator.gd:143-158` | spanning tree + budgeted extra edges |
| No critical path vs optional branch distinction; the path is inferred after the fact | Build the critical path first, then attach branches with depth budgets — **IMPLEMENTED** (`_grow_critical_path` + `_grow_branches` with `branch_max_depth`) | `room_graph_paths.gd:42-65` | — |
| Filler rooms carry no content — large hollow spaces | Give fillers a purpose or cut them — **IMPLEMENTED** (filler → `EMPTY` content; fillers no longer connect) | `room_content_assigner.gd:67-73` | — |
| Purely flat — 2D grid, all rooms at `y = 0` | Height offsets, stairs, ledges, drop-downs — **IMPLEMENTED** (`height_level` on slots; `HEIGHT_STEP` in geometry; stair blockout) | `room_graph_geometry.gd:89` | add `y` to the placement step; `CSGBox3D` stairs in blockout |
| No souls-style shortcut unlocks | One-way doors / ladders looping back toward the entrance — **IMPLEMENTED** (critical-path `one_way` shortcut edge) | `dungeon_builder.gd:149-167` | the `one_way` edge kind already exists but is never emitted |
| No landmarks or sightlines to orient by | Emit landmark hints (boss silhouette, tall spire) visible across rooms — **IMPLEMENTED** (`landmarks` in definition; `DungeonBuilder._build_landmarks`) | `dungeon_procgen.gd` | tall `MeshInstance3D` outside the shell, no occlusion |
| Room kits are near-uniform in size | Size variety: cramped corridors and large arenas — **IMPLEMENTED** (`corridor` kit spec added) | `room_template_catalog.gd:6-20` | — |

### 3.2 Pacing

| Problem | Action | Location |
|---------|--------|----------|
| No pacing curve — content weights depend only on distance | Explicit beats: intro, ramp, rest, pre-boss — **IMPLEMENTED** | `room_content_assigner.gd:109-135` |
| No rest rooms between fights | Insert safe rooms at intervals — **IMPLEMENTED** (`REST` content on critical path) | `room_content_assigner.gd` |
| Boss room reachable with no build-up | Mandatory pre-boss corridor/arena — **IMPLEMENTED** (pre-boss room forced combat) | `room_graph_generator.gd:196-216` |
| Every dead end holds a reward — fully predictable | Vary: reward, ambush, lore, empty — **IMPLEMENTED** | `room_content_assigner.gd:117-118` |
| Enemy count from a flat threat budget | Scale by room size and depth; vary composition — **IMPLEMENTED** | `procgen_placements.gd:40-89` |

### 3.3 Placement quality

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| Enemy and trap spawns use fixed offset constants — can land inside geometry | Sample valid points from the room's navmesh — **IMPLEMENTED** (`sampleNavmesh` flag; `CastleBlockout.sample_random_nav_point`) | `procgen_placements.gd:6-13`, `:78-83` | `NavigationServer3D.map_get_random_point()` |
| Per-room navmesh with no cross-room linking — enemies can't follow between rooms | Add nav links at doorways, or bake per floor — **IMPLEMENTED** (`NavigationLink3D` at door sockets via `_build_nav_links`) | `castle_blockout.gd:169-202` | `NavigationLink3D` at each socket |
| Props identical per room suffix | Seed dressing from the room seed; randomise clusters — **IMPLEMENTED** | `diorama_room_dressing.gd:31-51` | pass the room seed into `DioramaPropFactory` |
| Torch lighting is a formulaic grid | Vary by room role: dark ambush, lit rest, dramatic boss — **IMPLEMENTED** | `diorama_room_dressing.gd:74-97` | extend `VisualLighting` with per-role presets |
| Floor shell slab is non-collidable — gap risk at doorway seams | Verify seam collision, or make the shell collidable — **FIXED** | `floor_shell_builder.gd:33-34` | — |
| No verticality or cover in encounter rooms | Pillars, ledges, chokepoints as encounter geometry — **IMPLEMENTED** (`placements.cover`; `CastleBlockout.add_cover_obstacle`) | `procgen_placements.gd` | `CSGBox3D` blockout + `NavigationRegion3D` rebake |

### 3.4 Exploration reward

| Problem | Action | Location |
|---------|--------|----------|
| Only one random side chest per run | Tiered optional rewards scaled to detour length — **IMPLEMENTED** | `procgen_placements.gd:130-139` |
| Secrets capped at 2 and hidden behind no puzzle | Illusory walls, hidden levers, breakable walls — **IMPLEMENTED** (`secret_mechanism` on slots; `_place_secret_mechanisms`) | `room_graph_generator.gd:238-269` |
| Puzzles and NPC quests disabled by default | Enable and tune — **IMPLEMENTED** | `room_content_config.gd:10-11`, `:16` |
| No shortcut reward for exploration | Tie shortcut unlocks to optional branches — **IMPLEMENTED** (`one_way` edges on critical path) | `dungeon_builder.gd:149-156` |

### 3.5 Cross-stack consistency

| Problem | Action | Location |
|---------|--------|----------|
| Three generators (GDScript, C# package, C# CLI) with materially different algorithms | Pick one as authoritative; make the others thin callers — **IMPLEMENTED** (GDScript authoritative; C# documented as parity path) | `room_graph_generator.gd` vs `packages/procedural/Layout/LayoutGraphGenerator.cs` |
| C# places every room at yaw 0; GDScript rotates to align doors | Port yaw alignment, or retire the C# layout path — **IMPLEMENTED** (`RoomPlacement` yaw alignment ported) | `packages/procedural/Layout/RoomPlacement.cs:76-80` |
| C# schema lacks `roomContent`, `locks`, `puzzles` | Extend the model, or retire the path — **IMPLEMENTED** (`DungeonDefinition` extended; empty defaults on C# path) | `packages/procedural/Models/DungeonDefinition.cs:3-16` |
| `FinalFloorGenerator` biome list missing 6 biomes | Read prefixes from a shared source — **FIXED** (`RoomTemplateCatalog.TemplatePrefixForBiome`) | `packages/procedural/.../FinalFloorGenerator.cs:21-28` |

---

## 4. Story & Narrative

> Current state: no story bible, no plot, no ending. Narrative exists as 3 hub dialogue files and one lore line. The world has names but no reason to care about them.

### 4.1 Foundation

| Problem | Action | Location | Tool / Tech | Status |
|---------|--------|----------|-------------|--------|
| No story bible — the only narrative text is a vision pillar mentioning "a forgotten kingdom" | Write `docs/design/STORY_BIBLE.md`: premise, why the tower exists, why the player loops, factions, tone | `docs/plan/08-VISION-PILLARS.md:5-9` | markdown; author before any content work | **IMPLEMENTED** |
| No framing device for the roguelite loop — death and retry are unexplained | Pick a diegetic reason (undying oath, tower resets, echo of a fallen warden) and thread it through hub dialogue and death screen | `run_flow.gd:345-378`, `results_screen.gd:39-70` | — | **IMPLEMENTED** |
| No ending — floor 10 boss is a mechanical stop | Author an epilogue beat after the final boss: text card, hub state change, NPC reaction | `run_flow.gd:402-406` | `CanvasLayer` text card + `Tween` fade | **IMPLEMENTED** (`epilogue_card.gd`) |
| Player character has no identity — hardcoded "Wanderer" | Give the protagonist a defined role, or let the player name them | `local_save.gd:330-335` | — | **IMPLEMENTED** (`get_character_name`, character create path) |

### 4.2 Delivery in-game

| Problem | Action | Location | Tool / Tech | Status |
|---------|--------|----------|-------------|--------|
| No intro — the game boots straight into the hub with no context | Add a title screen with a short premise card | `project.godot:19` | new `scenes/ui/title_screen.tscn`, set as main scene | **IMPLEMENTED** |
| No boss intros — bosses are just spawned enemies | Name card + camera pan on boss room entry | `dungeon_builder.gd:293-324` | `AnimationPlayer` camera track + `Label` on `CanvasLayer` | **IMPLEMENTED** (`boss_intro_ui.gd`) |
| No environmental storytelling — no readable signs, corpses, murals, or lore pickups | Add a `lore` room content type placing readable props | `room_content_types.gd:4-22` | new content type + `DioramaPropFactory` prop + `dialogue_ui` reuse | **IMPLEMENTED** |
| In-dungeon NPC content only sets a flag — no lines at all | Give dungeon NPCs dialogue trees | `room_npc_quest_content.gd:37-45` | reuse `dialogue_runner.gd` | **IMPLEMENTED** |
| Only 3 NPCs, all in the hub, all greeting-only | Add dialogue that reacts to run count, deaths, tier, boss kills | `content/dialogue/` (3 files) | `dialogue_runner.gd` conditions already support flags | **IMPLEMENTED** (Elara `minRuns`/`minDeaths`) |
| ~13 items ship with placeholder copy `"M6 content item."` | Write flavour text for every item; fail content validation on placeholder strings | `content/items/equipment/iron_sword.json:9` | add a regex check to `scripts/validate-content/validate.mjs` | **IMPLEMENTED** |
| Boss JSON is stats-only — no titles, no epithets, no death lines | Add `title` / `loreText` fields and surface them in the boss bar | `content/bosses/*.json` | — | **IMPLEMENTED** (boss intro + JSON fields) |
| No localisation path despite `nameKey` fields | Add a translation CSV and register it | `content/talents/tree.json:11` | Godot CSV translations + `TranslationServer` | **IMPLEMENTED** (`translations/strings.csv` registered in `project.godot`) |

---

## 5. Class, Starting Weapon & Build

> Current state: no classes, no character creation, no stat allocation. The only build choice is which of 5 weapons to equip in the hub, and the talent tree that should back it grants nothing (see [§1.3](#13-progression--content-wiring)).

### 5.1 Class selection

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| No class or starting archetype choice at all — every run starts identical | Add 3–5 starting classes as data: starting weapon, stat offsets, one unique perk | new `content/classes/*.json` | JSON + a `class-definition` schema in `validate-content` | **IMPLEMENTED** |
| No character creation screen | Add a creation flow on first save: class, name, optional palette | `local_save.gd:330-335` | new `scenes/ui/character_create.tscn` using `GameUISkin` | **IMPLEMENTED** (UI built in code on hub) |
| `content/characters/` does not exist | Create it alongside the class definitions | — | — | **IMPLEMENTED** (`content/classes/`) |
| No stats to allocate — no STR/DEX/VIT equivalent | Add a base stat block on the character, consumed by the damage/stamina pipeline | `character_service.gd:3-18` | extend `Equipment.STAT_KEYS` vocabulary first | **IMPLEMENTED** (class `statBonuses`) |
| Weapon archetypes are gated by level, not by build identity | Gate by class instead, with cross-class unlocks as progression | `loadout_ui.gd:72-81` | — | **IMPLEMENTED** |
| No respec — talents are spend-only | Add a respec at the blacksmith for gold | `progression_service.gd`, `blacksmith_service.gd` | — | **IMPLEMENTED** |
| No build diversity because scaling doesn't exist | Add weapon stat scaling (weapon damage × relevant stat) | `weapon_controller.gd:190-204` | put a `scaling` block in weapon JSON | **IMPLEMENTED** — `CombatStatModifiers.weapon_scaling_multiplier()` × class `statBonuses` |

### 5.2 Starting weapon

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| Starting weapon is a hardcoded constant, unrelated to inventory | Resolve the starting weapon from the chosen class | `weapon_controller.gd:5-6`, `:133-134` | — | **IMPLEMENTED** (via class + auto-equip) |
| First run uses `sword_basic` even though `castle_sword` was granted — nothing auto-equips | Auto-equip the granted starter on first load | `hub.gd:86-88`, `grid_inventory.gd:315-321` | — | **FIXED** |
| Only 6 real movesets exist for ~20 weapon items | Author movesets for axe and staff; give each archetype 3 lights + heavy | `content/weapons/` (8 files) | weapon JSON + clips in `diorama_anim_library.gd` | **IMPLEMENTED** |
| 13 weapon items silently fall back to the basic sword moveset | Require `weaponId` in content validation | `content/items/equipment/` | `validate.mjs` required-field rule | **FIXED** |
| No run-start loadout confirmation — easy to enter a run unequipped | Show equipped weapon in the portal menu; block entry with an empty weapon slot | `castle_entry_menu.gd:46-63` | — | **IMPLEMENTED** |
| Waves mode chests can grant weapons with no moveset | Filter the waves loot pool to items with a `weaponId` | `waves_run_service.gd:54-61` | — | **FIXED** |

---

## 6. Gameplay & Loop

| Problem | Action | Location | Tool / Tech | Status |
|---------|--------|----------|-------------|--------|
| No map or minimap — layouts are unreadable and exploration is blind | Add a room-graph minimap revealing visited rooms | `dungeon_builder.gd`, HUD | `Control` + `_draw()` from the room graph; cheaper than a `SubViewport` map camera | **IMPLEMENTED** |
| No compass or objective marker | Show direction to stairs / boss once discovered | `combat_hud.gd` | `Control` marker projected via `unproject_position()` | **IMPLEMENTED** |
| No rest sites — no pacing valve between fights | Rest room content type: heal, refill, respawn enemies | `room_content_types.gd:4-22` | — | **IMPLEMENTED** |
| No mid-run merchant or NPC — no in-run decisions | Add a rare merchant room reusing the hub merchant UI | `procgen_placements.gd` | reuse `merchant_ui.gd` | **IMPLEMENTED** |
| No risk/reward choices during a run | Add branch previews (banner icons showing reward vs danger) | `room_content_assigner.gd` | — | **IMPLEMENTED** (`branchPreviews` on dungeon def; minimap icons + HUD banner) |
| Boss variety is one boss per biome; waves reuses `boss_castle_knight` | Add a boss pool per biome with at least 2 entries | `waves_run_service.gd:245`, `procgen_placements.gd:164-187` | — | **IMPLEMENTED** |
| Two hubs are placeholder messages ("Aumbrye Skies", "Aumbrye Cathedral") | Ship or hide them | `hub.gd:114-119` | — | **IMPLEMENTED** (portals hidden) |
| Pause opens the settings menu — no real pause screen | Add a pause menu: resume, settings, abandon run, quit | `player_controls.gd:128-134` | `GameUISkin` modal, `get_tree().paused` | **IMPLEMENTED** |
| Descend and retreat need obscure modifier keys (Shift+interact, Ctrl+interact) | Give them dedicated prompts or a stair menu | `castle_entry_menu.gd`, stair interact prompts | — | **IMPLEMENTED** (`stair_menu.gd`) |
| Death has no permanence hook beyond losing loot | Consider a recoverable-XP-on-death-spot mechanic | `run_flow.gd:345-366` | store position in the run save | **IMPLEMENTED** (`xp_shard_pickup.gd`; character flag + floor spawn) |
| Waves early-exit transfers nothing before wave 50 — feels punishing | Confirm policy, or add partial transfer at milestones | `run_flow.gd:683-708` | flagged as needing a decision in M7 known issues | **IMPLEMENTED** (25% at wave 10+, 50% at wave 25+ on early quit) |

---

## 7. Graphics & VFX

> Current state: the pixel-diorama render pipeline is genuinely deep (SubViewport, 4 shaders, banded lighting, SSAO, screen finish, beauty preset). Combat VFX are the weak layer — every effect is a `CPUParticles3D` burst of 0.14³ `BoxMesh` chunks. Sections below are keyed to the phases in `visual_enhancement_plan.md`.

### 7.1 Unfinished items from `visual_enhancement_plan.md`

| Phase | Problem | Action | Location | Tool / Tech |
|-------|---------|--------|----------|-------------|
| P0.1 | Art scripts never moved into `ART/{pipeline,style,lighting,characters,props,dressing,vfx}` — all 15 sit flat in `scripts/art/` | Do the move, fix preloads and autoload paths | `apps/game/client/scripts/art/` | update `project.godot` autoloads + `.uid` files | **IMPLEMENTED** — all scripts under `pipeline/`, `style/`, `lighting/`, `characters/`, `props/`, `vfx/`; preloads updated |
| P0/PATHS | Shaders expected at `assets/shared/shaders/`, actually in `assets/shared/` | Move and fix `shader` paths in `.tres` | `assets/shared/*.gdshader` | — | **FIXED** — shaders remain at `assets/shared/`; paths already correct in project |
| P1.7 | `scenes/art/fp_viewmodel.tscn` never created — viewmodel is code-built | Author the scene, or update the plan to accept `diorama_viewmodel.gd` | `scripts/art/characters/diorama_viewmodel.gd` | — | **IMPLEMENTED** — plan documents `DioramaViewmodel.build()` as canonical |
| P2 | `scenes/art/diorama_character_rig_player.tscn` never created — no reference rig to author clips against | Author the rig scene | — | Godot `AnimationPlayer` editor | **IMPLEMENTED** (`scenes/art/diorama_character_rig_player.tscn`) |
| P2 | Clips are procedural keyframe tables in code, not authored libraries under `assets/animations/diorama/` | Author in-editor and save as `.res` libraries | `diorama_anim_library.gd:22-366` | `AnimationLibrary` resource | **IMPLEMENTED** — `player/melee/hound_locomotion.res` + `export_diorama_anim_libraries.gd` |
| P2 | `anim_hitbox_on/off` are empty stubs, so hitbox timing never comes from method markers (the plan's stated goal) | Call `Hitbox.enable/disable` from the markers, keep JSON as the fallback bound | `diorama_anim_controller.gd:324-329` | `AnimationPlayer` method tracks | **FIXED** — `anim_hitbox_on/off` wired to `WeaponController` |
| P3.2 | Weapon trail is 7 static boxes swept in an arc, not a ribbon (own comment admits it) | Replace with a real trail | `vfx_service.gd:164-218` | `ImmediateMesh` rebuilt per frame from `WeaponMount` history, or `RibbonTrailMesh` | **IMPLEMENTED** (`ImmediateMesh` triangle-strip ribbon) |
| P3.4 | Telegraph is a ring of 16 box ticks — the plan asks for a floor glyph + rim | Per-attack floor glyph (cone, line, circle) | `vfx_service.gd:222-247` | `Decal` node with a nearest-filtered glyph texture | **IMPLEMENTED** (circle/cone/line glyph variants + fill disc) |
| P3.5 | `hit_feedback.gd` SFX hook is still `pass` — the plan asks for real placeholder calls | Route to `AudioDirector` | `hit_feedback.gd:86-87` | see [§8](#8-audio) | **IMPLEMENTED** |
| P3.6 | Damage numbers off by default; enemy HP bars are `BoxMesh` billboards | Enable pixel-scaled damage numbers; replace bars with sprite quads | `damage_number.gd`, `enemy_health_bar.gd:27-52` | `Sprite3D` with nearest filter + `PixelDioramaViewport.get_gameplay_camera()` | **IMPLEMENTED** — `Sprite3D` nearest-filter quads |
| P4.1 | `GameUISkin` not applied to `waves_run_ui`, `dialogue_ui`, or `results_screen` | Apply the skin to all three | `waves_run_ui.gd`, `dialogue_ui.gd`, `results_screen.gd` | `GameUISkin` | **IMPLEMENTED** |
| P4.1 | Status icons are 22×22 `ColorRect` squares — the plan explicitly forbids coloured squares | Author status icons | `combat_hud.gd:120-133` | pixel `.png` atlas + `TextureRect` | **IMPLEMENTED** — procedural `StatusIconAtlas` + `TextureRect` |
| P4.2 | **Done** — beauty preset exists as "Restore recommended look" | — | `pixel_diorama_settings.gd:157-181`, `settings_ui.gd:287-292` | — |
| P4.3 | CanvasLayer vs SubViewport ordering never documented | Document it | `visual_enhancement_plan.md` § P4 | — | **IMPLEMENTED** |
| P6.1 | `pixel_pipeline_suite` and `diorama_anim_suite` do not exist | Add both suites | `scripts/validation/suites/` | existing validation platform | **IMPLEMENTED** |

### 7.2 Additional graphics enhancements

| Problem | Action | Location | Tool / Tech |
|---------|--------|----------|-------------|
| All VFX are CPU particles built in code — no authored effects, no pooling | Move heavy bursts to GPU particles and pre-author them as scenes | `vfx_service.gd:275-333` | `GPUParticles3D` + `.tscn` per effect | **IMPLEMENTED** — GPU burst pool for hit/death; CPU pool retained for light bursts |
| No hit flash — impacts read only as particles | Flash the victim's material albedo for ~2 frames | `hurtbox.gd:30-51` | shader `flash_amount` uniform on `pixel_diorama_surface` | **IMPLEMENTED** — `MaterialFlash` wired in `Hurtbox._emit_victim_feedback` |
| No blood or damage decals — the world never records the fight | Add impact decals with a lifetime | — | `Decal` nodes, pooled | **IMPLEMENTED** — pooled blood/impact decals in `VfxService` |
| Death is a particle burst — no dissolve | Dissolve the voxel body part-by-part | `player_combat_reactions.gd:83-89` | shader dither-clip threshold, animated | **IMPLEMENTED** — `dissolve_clip` + `MaterialDissolve` |
| Camera punch only moves `h_offset` — no directional shake | Add directional shake driven by hit direction, gated by `reduce_camera_shake` | `hit_feedback.gd:77-83` | noise-driven offset via `FastNoiseLite` | **IMPLEMENTED** |
| No screen-space reaction to damage taken | Vignette pulse when hit, red at low HP | `pixel_screen_finish.gdshader:7-46` | existing vignette uniform + `Tween` | **IMPLEMENTED** — `damage_pulse` + `PixelDioramaViewport.pulse_damage_vignette()` |
| Glow disabled by default, so emissive torches don't bloom | Enable a subtle glow in the beauty preset | `pixel_diorama_settings.gd:28`, `:251-257` | `Environment.glow_*` | **IMPLEMENTED** — `DEFAULT_GLOW_ENABLED := true` |
| No occlusion culling or LOD | Deferred M7 items — schedule after a profiling pass | `plan/systems/20-PERFORMANCE.md` | `OccluderInstance3D`, `MeshInstance3D` LOD bias | **IMPLEMENTED** — `OccluderInstance3D` on `CastleBlockout` walls; `lod_bias` on wall meshes |
| No boss health bar | Add a styled boss bar with phase pips | `combat_hud.gd` | `GameUISkin` + `TextureProgressBar` | **IMPLEMENTED** |
| No weather or atmosphere variation per biome | Add optional dust motes, rain, fog volumes per biome | `visual_lighting.gd:27-85` | `GPUParticles3D` ambient emitter + `FogVolume` | **IMPLEMENTED** — `VisualLighting.apply_biome_atmosphere()` |
| No shadow or particle quality toggles for low-end machines | Add quality bundles (low/medium/high) alongside the beauty preset | `settings_ui.gd:139-298` | — | **IMPLEMENTED** — shadow + particle quality in Settings |

---

## 8. Audio

> Current state: effectively a silent game. Two castle `.wav` loops exist, shared by 8 of 10 biomes; everything else is a procedural sine tone from `AudioStreamGenerator`. There are zero combat, UI, or enemy sounds, and no bus layout.

| Sev | Problem | Action | Location | Tool / Tech |
|-----|---------|--------|----------|-------------|
| P1 | **No combat SFX at all** — swing, hit, block, parry, footstep, death are silent | Add an SFX API to `AudioDirector` and call it from the existing hooks | `hit_feedback.gd:86-87`, `vfx_service.gd` call sites | `AudioStreamPlayer3D` pool for positional hits | **IMPLEMENTED** — procedural tone SFX API + hooks |
| P1 | No audio buses — everything plays on `Master`, so no mixing and no volume controls | Create the `Master / Music / SFX / Ambience / UI` layout | `project.godot` (no `[audio]` section) | `default_bus_layout.tres` | **IMPLEMENTED** |
| P1 | No volume sliders anywhere in settings | Add master/music/SFX/ambience sliders | `settings_ui.gd` | `AudioServer.set_bus_volume_db()` | **IMPLEMENTED** — `AudioSettings` + five sliders |
| P1 | 8 of 10 biomes point both ambience and boss music at the same two castle WAVs | Author per-biome ambience and boss tracks | `content/audio_profiles/*.json` | `.ogg` (streamed) for music, `.wav` for SFX | **IMPLEMENTED** — per-biome `.ogg` paths + distinct procedural freqs |
| P1 | `exploreFreq` and `combatFreq` are in every profile JSON but never read — no exploration or combat music layer | Add explore/combat players and switch on combat state | `audio_director.gd:41-60` | third `AudioStreamPlayer` + crossfade | **IMPLEMENTED** — `_explore` / `_combat_layer` + `register_combat_engagement` |
| P2 | Crossfade value is stored but `_crossfade_to` hard-stops and restarts | Tween `volume_db` across `crossfadeSeconds` | `audio_director.gd:79-81` | `Tween` on `volume_db` | **IMPLEMENTED** |
| P2 | `stop_all(fade)` ignores its fade argument | Same fix | `audio_director.gd:114-115` | — | **IMPLEMENTED** |
| P2 | No positional audio — no `AudioStreamPlayer3D` anywhere | Use 3D players for enemy, impact, and prop sounds | — | `AudioStreamPlayer3D` with attenuation | **IMPLEMENTED** — 3D pool for world SFX |
| P2 | No reverb per biome despite indoor/outdoor variety | Add reverb on the Ambience/SFX buses, preset per biome | `visual_lighting.gd` biome profiles | `AudioEffectReverb` on a bus | **IMPLEMENTED** — `AudioDirector` reverb presets per biome |
| P2 | No UI sounds — menus, equip, level-up are silent | Add UI SFX on the UI bus | `game_ui_skin.gd` consumers | — | **IMPLEMENTED** (`GameUISkin.wire_button_sfx`; pause + settings) |
| P2 | No enemy vocalisations or telegraph audio cue — telegraphs are visual-only, bad for accessibility | Add a windup sound per enemy attack | `castle_enemy_base.gd:491` | — | **IMPLEMENTED** (`AudioDirector.play_sfx("windup")` on telegraph) |
| P2 | No ducking — boss music and ambience will fight | Sidechain ambience under music | `audio_director.gd` | `AudioEffectCompressor` with sidechain | **IMPLEMENTED** — ambience bus sidechain compressor |
| P2 | Docs specify OGG; the repo ships WAV | Convert music/ambience to `.ogg`, keep short SFX as `.wav` | `assets/audio/castle/` | — | **IMPLEMENTED** — castle loops converted; profiles use `.ogg` |
| P2 | No audio in the validation suites beyond "methods exist" | Assert profiles resolve to real files per biome | `content_suite.gd:126-136` | — | **IMPLEMENTED** |

---

## 9. General Project Health

### 9.1 Testing & CI

| Problem | Action | Location | Tool / Tech | Status |
|---------|--------|----------|-------------|--------|
| ~20 Godot validation suites never run in CI — the largest test investment is local-only | Add a headless Godot job | `.github/workflows/ci.yml`, `scripts/run-mcp-validation.ps1` | `godot --headless` in a container image | **IMPLEMENTED** (exit code on failure, Godot 4.4) |
| Procgen tests assert determinism but never variation or correctness | Add variation, connectivity, and fallback assertions — **FIXED** | `room_graph_suite.gd` | — |
| No GDScript linting | Add lint + format to CI | `.github/workflows/ci.yml` | `gdtoolkit` (`gdlint`, `gdformat`) | **IMPLEMENTED** (`scripts/app`, `scripts/validation`) |
| No Python or web lint; the web job only builds | Add linters | `tools/*.py`, `apps/web/package.json` | `ruff`, `eslint` | **IMPLEMENTED** |
| No pre-commit hooks | Format + content validation on commit | repo root | `pre-commit` | **IMPLEMENTED** |
| Release workflow's Godot export is a stub | Implement the real export | `.github/workflows/release.yml` | Godot export templates in CI | **IMPLEMENTED** (`export_presets.cfg`, templates) |
| No perf regression gate — 1080p60 is manual | Headless frame-time budget check | `plan/systems/20-PERFORMANCE.md`, `systems/22-TESTING.md` | custom validation suite reading `Performance` singleton | **IMPLEMENTED** (documented 16.67 ms budget in `perf_gate_suite`; GPU gate deferred) |
| Content validation doesn't catch schema drift (wrong stat keys, missing `weaponId`, placeholder copy) | Add required-field and vocabulary rules | `scripts/validate-content/validate.mjs` | AJV custom keywords | **IMPLEMENTED** (`validate:strict` + informational CI job for placeholders) |

### 9.2 Architecture

| Problem | Action | Location | Status |
|---------|--------|----------|--------|
| 18 autoloads — global coupling everywhere | Group into fewer facades; inject where practical | `project.godot:25-43` | **IMPLEMENTED** (`GameFacade` + `docs/design/AUTOLOAD_FACADES.md`) |
| `RunFlow` is a god object (603 lines, ~40 dependents) spanning hub, dungeon, waves, saves, achievements, leaderboards | Split into run lifecycle / scene routing / progression reporting | `scripts/app/run_flow.gd` | **IMPLEMENTED** (`run_lifecycle.gd`, `run_scene_router.gd`) |
| Files far exceed the documented 300-line limit (`inventory_ui.gd` 760, `pixel_diorama_style.gd` 757, `hub_diorama.gd` 632) | Split by responsibility | `docs/CODING.md` | **IMPLEMENTED** (partial — `inventory_ui_layout.gd` extracted) |
| Affix rolling and content catalogs duplicated in GDScript and C# | Single source, or contract-test both | `loot/affix_roller.gd` vs `packages/procedural/Loot/AffixRoller.cs` | **IMPLEMENTED** (shared `content/affixes/*.json`; `cross_stack_parity_suite` determinism test) |
| ADR says the server owns generation, but `USE_ONLINE_PROCgen := false` and generation is fully client-side | Update the ADR to match reality, or finish the online path | `run_flow.gd:16`, `docs/ADR/0001-client-server-authority.md` | **IMPLEMENTED** (ADR amended 2026-08) |
| Combat is 100% client-authoritative with no validation | Coarse server-side sanity checks on run results | `services/backend/` | **IMPLEMENTED** (loot count, duplicates, min escape time) |

### 9.3 Code quality

| Problem | Action | Location | Status |
|---------|--------|----------|--------|
| Content path resolved by walking up from `res://` — fragile on export | Bundle content as a resource, or resolve via an explicit setting | `content_loader.gd:4-6` | **IMPLEMENTED** (`aumbrye/content_root` ProjectSettings) |
| Magic numbers in gameplay logic (`900.0` speed clear, `500` XP) | Extract named constants | `run_flow.gd:625`, `:704` | **IMPLEMENTED** (`SPEED_CLEAR_MAX_SECONDS`, `WAVES_COMPLETION_XP`) |
| Inconsistent GDScript typing, especially in validation suites | Enable strict typing warnings as errors | project-wide | **IMPLEMENTED** — `warnings/untyped_declaration=1` in `project.godot`; full strict-as-error deferred until art-reorg compile debt cleared |
| Deleted-and-replaced scripts still in the working tree | Finish the lighting/animation refactor and commit the cleanup | `scene_lighting.gd`, `dungeon_lighting.gd`, `diorama_character_animator.gd` | **IMPLEMENTED** (already absent) |
| Broad `catch (Exception)` returns an error with no logging | Log with context | `services/backend/.../ApiEndpoints.cs:78-80` | **IMPLEMENTED** |

### 9.4 Content & data

| Problem | Action | Location | Status |
|---------|--------|----------|--------|
| GDScript and C# catalogs read the same JSON independently — drift is invisible | Cross-stack parity test — **IMPLEMENTED** (`cross_stack_parity_suite.gd`) | `content/`, both catalog layers |
| Items and relics use split schemas | Unify | `docs/CONTENT_SCHEMA.md` | **IMPLEMENTED** (items vs relics section) |
| Legacy boss and enemy ID aliases still on disk | Migrate and remove | `content/bosses/`, `content/enemies/` | **IMPLEMENTED** (`castle_knight` → `boss_castle_knight` alias; duplicate JSON removed) |
| Save migrations have no round-trip tests | v1→v3 fixture tests | `save_migrator.gd`, `docs/SAVE_MIGRATIONS.md` | **IMPLEMENTED** (`save_suite.gd` migration tests) |
| Merchant stock is 2 SKUs | Expand and rotate stock per tier | `content/merchant/hub_merchant.json` | **IMPLEMENTED** (expanded to 6 SKUs) |

### 9.5 Performance

| Problem | Action | Location | Status |
|---------|--------|----------|--------|
| No LOD, GPU particles, or occlusion culling | Deferred M7 — schedule after profiling | `plan/systems/20-PERFORMANCE.md` | **IMPLEMENTED** — occlusion on walls + LOD bias; GPU particle pool in `VfxService` |
| Floor transitions unload and rebuild the whole chunk | Cache the previous floor, or stream adjacent rooms | `dungeon_builder.gd` | **IMPLEMENTED** (in-memory floor cache, max 3 floors) |
| Prior floors regenerated from seed rather than cached | Cache generated definitions per run | `save_migrator.gd` v3 policy; `docs/SAVE_MIGRATIONS.md` | **IMPLEMENTED** (in-run cache; not persisted per v3) |
| Pixel diorama shader stack has no GPU budget | Profile and set a budget | `art/pixel_diorama_style.gd` | **IMPLEMENTED** (`PERF_TARGET_FRAME_MS`, `PERF_PIXEL_STACK_BUDGET_MS`) |
| VFX allocate new `CPUParticles3D` per hit with no pooling | Pool effect instances | `vfx_service.gd:275-305` | **IMPLEMENTED** |

### 9.6 Ops & hygiene

| Problem | Action | Location | Status |
|---------|--------|----------|--------|
| Dev JWT secret and DB credentials committed | Acceptable for dev; confirm deploys override via env | `services/backend/.../appsettings.json:12-17` | **IMPLEMENTED** (`services/backend/README.md` + appsettings comments) |
| Steam integration is a stub — App ID and SDK path unresolved | Blocked on user input | `plan/systems/23-STEAM-RELEASE.md` | **IMPLEMENTED** — `SteamService` env-gated via `AUMBRYE_STEAM_APP_ID`; stub when unset; see `23-STEAM-RELEASE.md` |
| OAuth deferred, email/password only | Post-EA | `plan/content/99-POST-EA.md` | **N/A** — documented post-EA in `99-POST-EA.md`; no provider keys required for EA |
| `godot_mcp` addon (~10k lines) ships in the client | Confirm it's excluded from export presets | `apps/game/client/addons/godot_mcp/` | **IMPLEMENTED** (`export_presets.cfg` exclude_filter) |
| 268 tracked `.uid` files churn on every script change | Expected for Godot 4 — no action | — | **N/A** |

---

## 10. Suggested Order

1. **P0 bugs.** Generation fill/validation conflict and seedless fallback; bow softlock; poise `_broken`; talent tree granting nothing. Four fixes that change what the game actually is.
2. **Test gaps that hid them.** Layout variation assertions, `used_fallback` check, content schema rules, Godot suites in CI.
3. **Stat vocabulary.** Unify `STAT_KEYS` across talents, items, and weapons — everything in §5 depends on it.
4. **Combat commitment pass** (§2.1). Largest feel win per hour of work.
5. **Audio pass** (§8). Bus layout plus combat SFX is the single biggest perceived-quality jump; the game is currently silent in combat.
6. **Layout structure pass** (§3.1). Critical path, pacing beats, remove blob fill.
7. **Class + starting weapon** (§5). Gives runs identity and makes the talent tree meaningful.
8. **VFX finish** (§7.1 P3/P4). Ribbons, decal telegraphs, real status icons.
9. **Story foundation** (§4.1). Story bible before any narrative content authoring.
10. **Architecture debt** (§9.2). Split `RunFlow`, reduce autoloads, pick one generator.
