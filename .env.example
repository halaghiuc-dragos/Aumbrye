# Docker Compose — copy to .env for local overrides (optional)

POSTGRES_USER=aumbrye
POSTGRES_PASSWORD=aumbrye_dev
POSTGRES_DB=aumbrye
POSTGRES_PORT=5432

REDIS_PORT=6379

# Backend (used when running API outside Docker)
DATABASE_URL=Host=localhost;Port=5432;Database=aumbrye;Username=aumbrye;Password=aumbrye_dev
REDIS_URL=localhost:6379
